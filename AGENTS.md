# 项目级 Agent 规范

本文件约束在本仓库中生成、整理和审校公务员考试备考内容的 Agent。仓库以 Markdown 为主，不引入运行时服务；所有题目都必须能在常见 Markdown 渲染器中阅读，并保留纯文本降级内容。

## 1. 任务边界

- 面向国考、省考的行测、申论、真题和备考指南内容，默认使用简体中文。
- 修改前先阅读相关目录说明、知识点笔记和真题解析，优先依据仓库内已有资料；不得凭题目名称猜测结论。
- 只做用户要求的内容生成或整理，不改写无关笔记，不执行构建、发布或其他外部副作用操作。
- 发现来源缺失、版本不明或题意无法确认时，停止生成该题并标记“待核验”，不得补造来源或答案。

## 2. 固定分类与文件布局

每道题必须且只能归入一个“大类”，取值固定为：

`行测`、`申论`、`真题`、`备考指南`

题目和答案分开保存，使用相同的文件基名，便于定向跳转：

```text
题库/
├─题目/<大类>/<主题>-题目.md
└─答案/<大类>/<主题>-答案.md
```

同一题在题目文件和答案文件中使用稳定、唯一的题号（如 `行测-数量关系-001`）。题目文件只能放题干、选项、来源和标签，不得出现正确选项、解析、答案提示或能反推出答案的加粗标记；答案文件集中放置正确选项、解析和来源核验。

## 3. 题目文件格式

优先使用 `90-模板/选择题模板.md`。每题必须包含以下信息：

1. 题号、题型（单选或多选）和固定“大类”。
2. 题干及 A、B、C、D（必要时 E）选项。
3. 可选择选项：使用通用 Markdown 任务列表（`- [ ]`）呈现选项，例如 `- [ ] A. 选项内容`。单选题和多选题分别在题型字段中标明；Markdown 任务列表不强制单选互斥，答案仍只存放在独立答案文件中。
4. `来源`字段：写明资料名称、发布机构或作者、年份/版本，以及可访问的 URL、仓库相对路径或页码/章节。仅写“网络资料”“教材”等笼统描述不合格。
5. `标签`字段：每题至少 3 个 `#标签`，其中必须包含与“大类”同名的固定标签（例如大类为“行测”时必须有 `#行测`），其余标签应描述科目、题型、知识点或难度。
6. `题目来源类型`字段：固定填写“真题”。本项目不生成原创题、改编题或变式题。
7. 指向答案文件中对应锚点的链接，例如：`[查看本题答案](../../答案/行测/数量关系-题目答案.md#行测-数量关系-001)`。链接文本不得泄露答案。

### 真题来源要求

- 所有题目必须是公开发布的真实考试题，不得使用模型原创、真题改写、变式或组合题补数。
- 每道真题必须标注考试类别、年份、地区/级别、科目、卷别（如适用）、题号和可访问的权威来源链接或出版物页码。
- 无法核验原题文本与答案一致性的题目，标记为“待核验”并停止提交；不得用“依据某资料”代替真题出处。
- 批量生成不足目标数量时宁可少生成，不得以原创题或改编题补齐。

选项示例（题号需替换为实际稳定题号）：

```markdown
- [ ] A. 选项内容
- [ ] B. 选项内容
```

## 4. 答案文件格式

答案文件使用 `90-模板/答案模板.md`。每题以同一题号作为 HTML 锚点（推荐 `<a id="行测-数量关系-001"></a>`），并依次提供：

- 正确选项及题型；
- 分步解析，说明判断依据，必要时给出公式、法条或材料定位；
- `来源核验`：再次列出与题目一致的具体真题出处，并核对题干、选项、答案和解析；
- `题目来源类型`：固定为“真题”；
- 与题目一致的全部标签，至少 3 个且包含固定大类标签。

答案文件可在文件开头提供“题号—答案”索引，但不得把答案回写到题目文件。题目链接必须能直接跳转到答案文件的对应锚点。

## 5. 来源与智能生成规则

- 来源优先级：政府/考试机构公告与大纲 > 正式教材或法规原文 > 仓库内带出处的笔记 > 其他可追溯资料。
- 涉及时效性政策、年份、数字或考试规则，必须标注发布日期/版本并核对当前适用范围；无法核对时标记“待核验”。
- “智能生成”只能表示基于已标注来源的改写、组合或变式，不得把模型推断当作事实。答案中的“生成依据”要指向具体来源。
- 每题只设置一个明确考查点；干扰项应与来源内容相关且可解释，避免依靠错别字或无关常识制造区分度。
- 生成前检查题号唯一性、选项数量、答案唯一性（多选题除外）、来源可追溯性、标签数量和链接锚点；任一项不满足就不提交文件。

## 6. 真题同水平质量门槛

生成题目必须达到对应考试真题的作答质量，不以“形式完整”代替“考查有效”。每题提交前逐项核验：

- **考点与难度**：考点对应明确的考试大纲或真题专题；推理链、计算量、阅读量和干扰强度与同类真题相当，不得出现一眼可排除、无需理解材料或依赖生僻冷知识的题目。
- **题干与设问**：题干信息完整、条件无歧义，设问符合行测/申论常见命题方式；不得通过缺字、病句、标点或不自然措辞制造难度。
- **选项与干扰项**：单选题只有一个可由来源和题干共同推出的正确项；干扰项须具有真实迷惑性，分别对应常见误读、范围偷换、因果倒置、概念混淆或计算错误等可解释的错误路径，长度和表达风格基本均衡。
- **材料与答案**：材料、题干、选项和解析之间能够相互定位；解析说明关键判断步骤和排除依据，不得只给结论。计算题须展示必要公式和单位，政策/法律题须标明适用版本。
- **语言与规范**：使用正式、简洁、无歧义的考试书面语；数字、日期、专名、引文和逻辑连接词准确，避免口语化、营销化和模型腔表达。
- **原题完整性**：不得改变真题材料、数据、设问、选项或答案；如官方存在不同卷别或勘误，必须标注具体版本，不能自行拼接。
- **淘汰规则**：任一质量项无法证明通过，题目标记为“待核验”并退回重写，不得因凑数、配比或批次截止而提交。

## 7. 审校清单

- 题目文件不含答案或解析，答案文件独立存在且路径正确。
- 所有选项均使用 `- [ ]` 任务列表语法；选项字母和顺序清晰，题型标记与答案数量一致。
- 每题“大类”值合法，标签不少于 3 个并含固定大类标签。
- 每道题来源类型均为“真题”，且具体出处可独立核验；不接受原创题、改编题或变式题。
- 题目和答案均有具体来源；链接、相对路径和锚点可解析。
- 注释、解析和来源只描述已核实内容；发现潜在事实错误、版权或时效风险时，在交付说明中单独列出。

## 8. 交付说明

完成生成后，汇报新增/修改的题目文件和答案文件、题目数量、来源核验情况、标签与任务列表选项检查结果、真题同水平质量核验结果，以及未执行的运行时验证。不要声称进行了浏览器交互或构建，除非确实执行并记录结果。
